export const orderAmountCalc = (cart,adress,userId,discount) => ()=> {
// item price(product price * quantity)
  // calc subTotal(sum of products in cart)
  // calculate shipping cost
  // minus discount
  return {grandTotal, subTotal, shippingCost,amountAfterDiscount};

}